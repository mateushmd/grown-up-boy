#include <cstdint>

#include "lcd.h"

namespace emulator::io {
    uint8_t LCD::read(const uint16_t address) const {
        TODO();
    }

    void LCD::write(const uint16_t address, const uint8_t value) {
        TODO();
    }
}
