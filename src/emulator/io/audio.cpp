#include <cstdint>

#include "audio.h"

namespace emulator::io {
    uint8_t Audio::read(const uint16_t address) const {
        TODO();
    }

    void Audio::write(const uint16_t address, const uint8_t value) {
        TODO();
    }
}
