#include "window.hpp"

#include <string>

namespace Devices
{
    /*
    bool Window::init()
    {
        if (!SDL_Init(SDL_INIT_VIDEO))
        {
            SDL_Log("Could not initalize SDL VIDEO. SDL error: %s", SDL_GetError());
            return false;
        }

        window = SDL_CreateWindow("Grown Up Boy", WIDTH, HEIGHT, 0);

        if (window == nullptr)
        {
            SDL_Log("Could not create window. SDL error: %s", SDL_GetError());
            return false;
        }

        surface = SDL_GetWindowSurface(window);

        return true;
    }*/
}
